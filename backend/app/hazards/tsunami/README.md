# Tsunami Hazard Module Placeholder

This folder is intentionally inactive. A future tsunami hazard can be added by
creating its own `plugin.py`, `router.py`, `schemas.py`, `service.py`,
`repository.py` if persistence is needed, `calculator.py`, and `formulas.py`.

Do not place tsunami formulas or route behavior inside the earthquake module.

